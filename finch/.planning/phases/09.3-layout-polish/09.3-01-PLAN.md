---
phase: 09.3-layout-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/dashboard/Dashboard.tsx, src/components/sidebar/RightSidebar.tsx, components/ui/sidebar.tsx]
autonomous: true
requirements: [R-09.3-01, R-09.3-02, R-09.3-03]

must_haves:
  truths:
    - "Dashboard has three clear vertical layers: Top Bar, Second Bar, and Zone"
    - "Top Bar is the only area with data-tauri-drag-region"
    - "Left sidebar toggle uses SVG assets and controls visibility correctly"
    - "RightSidebar background matches the left sidebar (bg-sidebar)"
    - "Model Selector and Unload button are perfectly inline"
    - "Sidebars are constrained within the Zone and do not overlap the bars"
  artifacts:
    - path: "src/components/dashboard/Dashboard.tsx"
      provides: "New layout structure"
    - path: "src/components/sidebar/RightSidebar.tsx"
      provides: "Polished sidebar styling"
  key_links:
    - from: "src/components/dashboard/Dashboard.tsx"
      to: "public/assets/open-state-left.svg"
      via: "img tag"
---

<objective>
Restructure the Dashboard layout to follow a three-tier vertical design (Top Bar, Second Bar, Zone) and polish the RightSidebar to match the application's design system. This ensures a professional, non-overlapping UI with correct drag regions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/dashboard/Dashboard.tsx
@src/components/sidebar/RightSidebar.tsx
@components/ui/sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish RightSidebar and Update Sidebar Component</name>
  <files>src/components/sidebar/RightSidebar.tsx, components/ui/sidebar.tsx</files>
  <action>
    - In `src/components/sidebar/RightSidebar.tsx`:
      - Change background from `bg-background/50 backdrop-blur-md` to `bg-sidebar`.
      - Ensure `framer-motion` width animation uses `320px` for open and `0px` for closed.
    - In `components/ui/sidebar.tsx`:
      - Ensure the `Sidebar` container (data-slot="sidebar-container") can be switched from `fixed` to `absolute` via props or by ensuring `className` overrides work correctly with `tailwind-merge`.
      - Actually, modify the `Sidebar` component to accept a `className` that can override `fixed inset-y-0 h-svh` with `absolute inset-y-0 h-full` when needed.
  </action>
  <verify>
    <automated>grep -q "bg-sidebar" src/components/sidebar/RightSidebar.tsx</automated>
  </verify>
  <done>RightSidebar matches design system and Sidebar component supports nested layout.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure Dashboard.tsx Layout</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <action>
    - Refactor `Dashboard` root to `flex flex-col h-screen w-full overflow-hidden`.
    - Create TOP BAR (header):
      - `data-tauri-drag-region`, `h-14`, `sticky top-0`, `z-20`.
      - Left: Button with `open-state-left.svg` / `closed-state-left.svg` controlling a new `isLeftSidebarOpen` state.
      - Right: `Incognito Toggle`, `Bell` icon (Lucide), `Separator`, `Switch` (theme), `WindowControls`.
    - Create SECOND BAR (div):
      - `h-12`, `border-b`, `no-drag`, `z-10`.
      - Center: `ModelSelector` and `Unload` button (LogOut icon) in an inline flex group (`flex items-center gap-2`).
      - Right: `RightSidebar` toggle button using SVG assets.
    - Create ZONE (div):
      - `flex flex-1 overflow-hidden relative`.
      - Wrap `ChatSidebar` and `main` chat area in `SidebarProvider` with `className="min-h-0"` and controlled `open`/`onOpenChange` linked to `isLeftSidebarOpen`.
      - `ChatSidebar` should pass `className="absolute inset-y-0 h-full"` to stay within the Zone.
      - `RightSidebar` as a sibling to `SidebarProvider`.
    - Ensure `isIncognito` styling (borders, margins) is applied correctly to the `main` element within the Zone.
    - Move `SidebarIncognitoController` inside the `SidebarProvider` to wrap `main`.
  </action>
  <verify>
    <automated>grep -q "flex flex-col h-screen" src/components/dashboard/Dashboard.tsx</automated>
  </verify>
  <done>Dashboard layout follows the three-tier vertical structure.</done>
</task>

<task type="auto">
  <name>Task 3: Refine Drag Regions and Component Alignment</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <action>
    - Remove `data-tauri-drag-region` from the second bar and any other elements besides the top header.
    - Ensure the `ModelSelector` and `Unload` button are perfectly centered and inline.
    - Verify `Bell` icon is imported from `lucide-react`.
    - Ensure `isLeftSidebarOpen` state is initialized to `true`.
  </action>
  <verify>
    <automated>grep "data-tauri-drag-region" src/components/dashboard/Dashboard.tsx | wc -l</automated>
  </verify>
  <done>UI interactions and drag regions are correctly restricted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri → UI | Window control events and drag region interactions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.3-01 | Denial of Service | Dashboard Layout | mitigate | Use overflow-hidden and min-h-0 to prevent layout blowouts in nested flex containers. |
| T-09.3-02 | Information Disclosure | Incognito Mode | mitigate | Ensure sidebars and bars respect isIncognito state consistently after restructuring. |
</threat_model>

<verification>
1. Check that the top bar is draggable.
2. Check that the second bar is NOT draggable.
3. Check that toggling the left sidebar only shifts the chat area and not the header.
4. Check that toggling the right sidebar only shifts the chat area and not the header.
5. Verify RightSidebar background color.
</verification>

<success_criteria>
- Dashboard restructure complete.
- Left and Right sidebar toggles functional with SVG assets.
- Drag region restricted to top bar.
- Incognito mode visuals preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/09.3-layout-polish/09.3-01-SUMMARY.md`
</output>
