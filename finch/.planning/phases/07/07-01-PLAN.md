---
plan: "07-01"
name: Chat Infrastructure & Settings Migration
description: Implement Rust persistence commands and migrate user profile/settings to tauri-plugin-store (finch_config.json).
phase: 07
type: autonomous
wave: 1
depends_on: ["06-02"]
files_modified:
  - "src-tauri/src/lib.rs"
  - "src-tauri/permissions/finch.toml"
  - "src-tauri/capabilities/default.json"
  - "src/types/chat.ts"
  - "src/hooks/useChatPersistence.ts"
  - "src/components/dashboard/Dashboard.tsx"
autonomous: true
requirements:
  - "R-11: Last used model must persist between sessions."
  - "R-12: Chats must be stored in individual JSON files via Rust commands."
  - "R-13: All user profile data and preferences must migrate from localStorage to finch_config.json."
must_haves:
  truths:
    - "Infrastructure: Rust commands list/load/save/delete implemented"
    - "Settings: Profile and preferences moved to tauri-plugin-store (finch_config.json)"
  artifacts:
    - file: "src-tauri/src/lib.rs"
      provides: "list_chats, load_chat, save_chat, delete_chat commands; renamed store to finch_config.json"
      min_lines: 300
    - file: "src-tauri/permissions/finch.toml"
      provides: "Command authorizations"
      min_lines: 30
  key_links:
    - "Dashboard.tsx -> finch_config.json (via store)"
---

# Phase 07-01: Chat Infrastructure & Settings Migration

## Tasks

<task>
<name>RUST PERSISTENCE: INFRASTRUCTURE & COMMANDS</name>
<files>
  - "src-tauri/src/lib.rs"
  - "src-tauri/permissions/finch.toml"
  - "src-tauri/capabilities/default.json"
</files>
<action>
- Rename `settings.json` to `finch_config.json` in all Rust store calls.
- Update `AppState` to include the `AppHandle` or cached data directory path.
- Implement directory initialization (ensure `finch/chats` exists) in `run()`.
- Add `list_chats`, `load_chat`, `save_chat`, `delete_chat` commands.
- Implement atomic write utility: write to `<uuid>.json.tmp`, then `std::fs::rename` to `<uuid>.json`.
- `save_chat` logic: if ID is empty/missing, generate a new UUID; return the (new or existing) ID.
- Register commands in `invoke_handler`.
- Update `finch.toml` with `allow-*` permissions for the 4 new commands.
- Update `capabilities/default.json` with the new permissions.
</action>
<verify>
- Check `src-tauri/src/lib.rs` for command implementations and renamed store.
- <automated>powershell -NoProfile -Command "Get-Content src-tauri/src/lib.rs | Select-String -Pattern 'finch_config\.json'"</automated>
</verify>
<done>Rust backend commands and renamed configuration store are implemented.</done>
</task>

<task>
<name>DATA TYPE UPDATES</name>
<files>
  - "src/types/chat.ts"
</files>
<action>
- Update `ChatSession` type to include optional `model` and `provider` fields at the root level for easier metadata indexing in `list_chats`.
</action>
<verify>
- Confirm `ChatSession` in `src/types/chat.ts` has the new fields.
- <automated>powershell -NoProfile -Command "Get-Content src/types/chat.ts | Select-String -Pattern 'model\?: string;'"</automated>
</verify>
<done>TypeScript types are aligned with the new persistence requirements.</done>
</task>

<task>
<name>USER SETTINGS MIGRATION (PROFILE & PREFS)</name>
<files>
  - "src/hooks/useChatPersistence.ts"
  - "src/components/dashboard/Dashboard.tsx"
</files>
<action>
- Move `profileName`, `profileEmail`, and `enterToSend` from `localStorage` to `tauri-plugin-store` (same store used for provider config).
- Update `Dashboard.tsx` to load these values from the store on mount.
- Ensure all settings changes (including `selectedModel` and `selectedProvider`) trigger a store update and save.
- **Explicitly remove all localStorage reads and writes** from these files.
</action>
<verify>
- Change profile name and model, restart, and verify persistence in the new store.
- <automated>powershell -NoProfile -Command "Get-Content src/hooks/useChatPersistence.ts | Select-String -Pattern 'localStorage'" | ForEach-Object { if ($_) { throw 'localStorage found in useChatPersistence.ts' } }</automated>
</verify>
<done>User profile, preferences, and last-used model are migrated to the robust Tauri store; localStorage is purged.</done>
</task>

## Verification
- **Build & Run**: Ensure the application builds successfully with `npm run build`.
- **Settings Persistence**: Verify profile name, email, and enter-to-send survive an app restart.
- **No localStorage Audit**: Run a grep across the entire `src/` directory to ensure no `localStorage` remains.
