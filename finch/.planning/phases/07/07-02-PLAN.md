---
plan: "07-02"
name: Chat Persistence Refactor & Migration
description: Implement chat history migration from localStorage to JSON files, fix duplicate bug, and complete R-08 stats UI.
phase: 07
type: autonomous
wave: 2
depends_on: ["07-01"]
files_modified:
  - "src/hooks/useChatPersistence.ts"
  - "src/components/dashboard/Dashboard.tsx"
  - "src/components/chat/MetadataRow.tsx"
autonomous: true
requirements:
  - "R-08: Message stats must show real-time performance metrics (stop_reason, total_tokens, tokens/s) with hover-reveal."
  - "R-09: Incognito chats must not persist after session ends."
  - "R-10: Messages must append to existing chats without duplication."
must_haves:
  truths:
    - "Persistence: localStorage chats moved to individual files"
    - "Correctness: No duplicate entries on first message send"
    - "Cleanup: Zero instances of localStorage remaining in codebase"
  artifacts:
    - file: "src/hooks/useChatPersistence.ts"
      provides: "Migration logic from localStorage to Rust"
      min_lines: 50
    - file: "src/components/chat/MetadataRow.tsx"
      provides: "Hover-reveal performance stats with motion"
      min_lines: 80
  key_links:
    - "Dashboard.tsx -> useChatPersistence.ts (Migration)"
---

# Phase 07-02: Chat Persistence Refactor & Migration

## Tasks

<task>
<name>CHAT DATA MIGRATION & REFACTOR</name>
<files>
  - "src/hooks/useChatPersistence.ts"
  - "src/components/dashboard/Dashboard.tsx"
</files>
<action>
- Gut remaining chat logic in `useChatPersistence.ts`.
- Implement **Chat Migration**:
  - Check `localStorage` for `finch_chats` on first run.
  - Iterate and call `save_chat` in Rust for each legacy session.
  - Clear `finch_chats` from `localStorage` once complete.
- Update `Dashboard.tsx` to use `list_chats` and `load_chat` from Rust.
- Fix **Duplicate Bug**:
  - Update `updateActiveSessionInList` to await the result of `save_chat`.
  - Set `activeSessionId` ONLY from the Rust-generated response.
- Explicitly filter `incognito` chats from all `save_chat` calls (`R-09`).
</action>
<verify>
- Confirm `activeSessionId` is set from Rust response.
- Verify migration by checking the `finch/chats` directory for converted files.
- <automated>powershell -NoProfile -Command "Get-Content src/components/dashboard/Dashboard.tsx | Select-String -Pattern 'activeSessionId = await'"</automated>
</verify>
<done>Chat history is fully migrated to files; duplicates are eliminated.</done>
</task>

<task>
<name>MESSAGE STATS UI (R-08)</name>
<files>
  - "src/components/chat/MetadataRow.tsx"
</files>
<action>
- Implement hover-reveal UI for performance metrics using Framer Motion.
- Everything Animates: Use `AnimatePresence` and `motion.div` for entry/exit (fade + slide up 12px, 200ms).
- Micro-interactions: Ensure the metadata row itself has a soft hover lift (translateY -2px).
- Stats display: Show `totalTokens`, `tokensPerSecond`, and `stopReason` clearly when the user hovers over the metadata row.
</action>
<verify>
- Hover over a message's metadata and confirm stats appear with motion.
- <automated>powershell -NoProfile -Command "Get-Content src/components/chat/MetadataRow.tsx | Select-String -Pattern 'AnimatePresence'"</automated>
</verify>
<done>Message performance stats are visible via a polished hover-reveal interaction with motion.</done>
</task>

<task>
<name>FINAL LOCALSTORAGE PURGE</name>
<files>
  - "src/**/*.tsx"
  - "src/**/*.ts"
</files>
<action>
- Final search for any remaining `localStorage` usage in the frontend.
- Remove or migrate any missed instances.
- Ensure `useChatPersistence.ts` no longer references `localStorage` after migration.
</action>
<verify>
- Run a codebase-wide grep for `localStorage`.
- <automated>powershell -NoProfile -Command "Get-ChildItem src -Recurse -Include *.ts,*.tsx | Select-String -Pattern 'localStorage' -Quiet" | ForEach-Object { if ($_) { throw 'localStorage still exists in src/' } }</automated>
</verify>
<done>The codebase is 100% free of localStorage dependencies.</done>
</task>

## Verification
- **Persistence**: Restart the app and confirm the chat list is correctly populated from files.
- **Bug Fix**: Start a new chat, send one message, and verify only ONE entry appears in the sidebar.
- **UI Test**: Verify stats hover-reveal with motion.
- **Audit**: Confirm zero `localStorage` matches in the entire project.
