---
plan: "06-01"
name: Sidebar & Input Refinement
description: Polish sidebar interaction, web search toggle, and dark mode toasts.
phase: 06
type: autonomous
wave: 1
depends_on: ["05-02"]
files_modified:
  - "src/components/sidebar/ChatSidebar.tsx"
  - "src/components/chat/ChatInput.tsx"
  - "src/App.tsx"
autonomous: true
requirements:
  - "R-05: Sidebar active chats must use violet accent and clear distinction."
  - "R-06: Web search toggle must use blue stroke and border glow."
  - "R-07: Dark mode toasts must be frosted semi-transparent."
must_haves:
  truths:
    - "Accent: oklch(0.488 0.243 264.376) @ 15% opacity"
    - "Toasts: backdrop-blur-md, neutral-900/90"
  artifacts:
    - file: "src/components/sidebar/ChatSidebar.tsx"
      provides: "Sidebar active highlights and AI name indicators"
      min_lines: 200
    - file: "src/components/chat/ChatInput.tsx"
      provides: "Globe toggle stroke and border glow"
      min_lines: 100
    - file: "src/App.tsx"
      provides: "Frosted toasts in dark mode"
      min_lines: 10
  key_links:
    - "ChatSidebar.tsx -> Dashboard.tsx via activeSessionId"
---

# Phase 06-01: Sidebar & Input Refinement

## Tasks

<task>
<name>SIDEBAR CHAT INDICATORS</name>
<files>
  - "src/components/sidebar/ChatSidebar.tsx"
</files>
<action>
- Implement `isAiNamed` heuristic logic.
- Style AI titles with `italic text-muted-foreground/80`.
- Update `isActive` pill to violet accent `bg-[oklch(0.488_0.243_264.376)]/15`.
- Remove icon fill on active state.
</action>
<verify>
- Observe active chat highlight in sidebar.
- Confirm AI-named chats appear with distinct styling.
</verify>
<done>Active chat is highlighted with violet accent; AI names are visually distinct.</done>
</task>

<task>
<name>WEB SEARCH TOGGLE & GLOW</name>
<files>
  - "src/components/chat/ChatInput.tsx"
</files>
<action>
- Apply `text-blue-500` stroke to `Globe` when active; remove solid fill.
- Add `ring-1.5 ring-blue-500/50` and `border-blue-500/50` to input container when `isWebSearchActive`.
</action>
<verify>
- Toggle web search button.
- Confirm blue stroke and border glow activation.
</verify>
<done>Web search active state is indicated by blue stroke and border glow.</done>
</task>

<task>
<name>FROSTED TOASTS & DARK MODE</name>
<files>
  - "src/App.tsx"
</files>
<action>
- Update `Toaster` with `bg-neutral-900/90 backdrop-blur-md border border-white/10 text-white` styles.
</action>
<verify>
- Trigger a toast in dark mode.
- Confirm frosted look with backdrop blur.
</verify>
<done>Toasts have a dark semi-transparent frosted look in dark mode.</done>
</task>

## Verification
- Run `npm run dev` and visually verify all tasks.
- Check build with `npm run build`.
