---
phase: 09-right-sidebar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/Dashboard.tsx
  - src/components/sidebar/RightSidebar.tsx
autonomous: true
requirements: [R-17, R-18]
must_haves:
  truths:
    - "User can click the toggle button in the top right to open the right sidebar"
    - "User can click the toggle button again to close the right sidebar"
    - "When open, the sidebar is a fixed width and compresses the chat area"
    - "When closed, the sidebar has zero width and the chat area expands"
  artifacts:
    - path: "src/components/dashboard/Dashboard.tsx"
      provides: "Sidebar toggle state and button"
    - path: "src/components/sidebar/RightSidebar.tsx"
      provides: "Sidebar shell component"
  key_links:
    - from: "src/components/dashboard/Dashboard.tsx"
      to: "src/components/sidebar/RightSidebar.tsx"
      via: "isOpen prop"
---

<objective>
Add a collapsible right sidebar shell to the app layout with a toggle button.

Purpose: Prepares the layout structure for future right sidebar content without affecting current chat functionality.
Output: Working toggle button, animated shell sliding in from the right, layout properly compressing.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing dashboard
@src/components/dashboard/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RightSidebar shell component</name>
  <files>src/components/sidebar/RightSidebar.tsx</files>
  <action>
    Create a new React component `RightSidebar` that receives an `isOpen: boolean` prop.
    
    Implementation details:
    - Use `framer-motion` (`motion.aside`) for the root element.
    - Set up the animation for width: `initial={false}`, `animate={{ width: isOpen ? 320 : 0, opacity: isOpen ? 1 : 0 }}`.
    - Add transition: `{ duration: 0.3, ease: 'easeInOut' }` to match the native feel.
    - Important classes: `flex-shrink-0 overflow-hidden bg-background/50 backdrop-blur-md border-l border-muted/20 z-20 h-full flex flex-col`.
    - Do NOT add any content inside the sidebar. Leave it as an empty shell.
    - Export the component as a named export.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>RightSidebar component exists, uses framer-motion, and cleanly handles width animation based on the isOpen prop.</done>
</task>

<task type="auto">
  <name>Task 2: Wire RightSidebar into Dashboard layout and add toggle button</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <action>
    Update the layout to include the right sidebar and its toggle button.
    
    1. Import `RightSidebar` from `@/src/components/sidebar/RightSidebar`.
    2. Add state `const [isRightSidebarOpen, setIsRightSidebarOpen] = useState(false);`.
    3. Find the header section inside `<main>` (the `flex items-center gap-2 mr-4 no-drag` div).
    4. Insert a toggle `<Button>` before `<WindowControls />`.
       - Button props: `variant="ghost" size="icon" onClick={() => setIsRightSidebarOpen(!isRightSidebarOpen)} className="text-muted-foreground"`.
       - Button content: Use an `<img>` tag with `src` pointing to the pre-existing SVG assets based on state:
         `isRightSidebarOpen ? "/assets/open-state-right.svg" : "/assets/closed-state-right.svg"`.
       - Add `className="h-5 w-5"` and `alt="Toggle Right Sidebar"` to the `<img>`.
    5. Render the `<RightSidebar isOpen={isRightSidebarOpen} />` component as a sibling to the `<main>` element, INSIDE the `<SidebarIncognitoController>` wrapper.
       - Note: The current structure is `div.flex.h-screen > ChatSidebar + SidebarIncognitoController > main`.
       - Change it to: `div.flex.h-screen > ChatSidebar + SidebarIncognitoController > div.flex.flex-1.min-w-0.min-h-0 > main + RightSidebar`.
       - OR simply render `<RightSidebar isOpen={isRightSidebarOpen} />` adjacent to `<main>` inside an updated parent wrapper if needed, to ensure they sit side-by-side filling the remaining width after `ChatSidebar`. `SidebarIncognitoController` returns `<>{children}</>`, so you can wrap `main` and `RightSidebar` in a `<div className="flex-1 flex min-w-0 min-h-0">` and change `<main>`'s classes to `flex-1 flex flex-col min-w-0 min-h-0 relative...`. Wait, `<main>` already has `flex-1`. So changing `SidebarIncognitoController` children to a fragment with `<main... />` and `<RightSidebar... />` side-by-side is enough, but they need to be in a flex row. `div.flex.h-screen` is already a flex row! So placing `<RightSidebar>` right after `<main>` (inside `<SidebarIncognitoController>`) will automatically place it to the right of `<main>`, and `<main>` being `flex-1` will shrink/grow!
       - Yes, just place `<RightSidebar isOpen={isRightSidebarOpen} />` as the next sibling to `<main>`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Dashboard renders the toggle button with the correct SVG states, and the RightSidebar is rendered side-by-side with main, functioning correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI State | React component state within the local client. No external data crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | RightSidebar | accept | The sidebar is currently an empty shell and handles no user data or state other than open/close. No mitigation required. |
</threat_model>

<verification>
- Verify that clicking the toggle button changes its SVG icon between open and closed states.
- Verify that the sidebar smoothly slides in from the right when toggled open, taking up ~320px width.
- Verify that the main chat area compresses cleanly to fit the remaining space without overlapping the new sidebar.
- Verify that no chat logic, message layout, or application behavior is broken.
</verification>

<success_criteria>
- The right sidebar functions entirely as an empty, animated shell that can be toggled via the new top-right button.
- Layout remains fully responsive and native-feeling, respecting existing glassmorphism and animation standards.
</success_criteria>

<output>
After completion, create `.planning/phases/09-right-sidebar/09-01-SUMMARY.md`
</output>
