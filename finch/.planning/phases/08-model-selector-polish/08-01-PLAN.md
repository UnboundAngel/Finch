---
phase: "08-model-selector-polish"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["src/components/chat/ModelSelector.tsx"]
autonomous: false
requirements: ["R-13", "R-14", "R-15"]

must_haves:
  truths:
    - "Bookmarked models appear in the Bookmarked section"
    - "Bookmarked models do NOT appear in their original provider sections"
    - "Unbookmarked models reappear in their original provider sections"
    - "Refreshing models maintains these rules"
  artifacts:
    - path: "src/components/chat/ModelSelector.tsx"
      provides: "Exclusive bookmark visibility logic"
  key_links:
    - from: "src/components/chat/ModelSelector.tsx"
      to: "bookmarkedModels state"
      via: "filtering in providers.map"
---

<objective>
Implement exclusive visibility for bookmarked models in the ModelSelector.
Bookmarked models should move from their provider section to the "Bookmarked" section.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/chat/ModelSelector.tsx
</context>

<tasks>

<task type="auto">
  <name>Implement exclusive bookmark filtering</name>
  <files>src/components/chat/ModelSelector.tsx</files>
  <action>
    Modify the rendering logic in `ModelSelector.tsx`:
    1. Inside the `providers.map` loop, filter the `provider.models` array.
    2. A model should be excluded from the provider section if it exists in the `bookmarkedModels` state.
    3. Ensure the `Bookmarked` section remains as is (it already correctly displays all bookmarked models).
    4. Ensure that if a provider has no non-bookmarked models, its section header and separator are correctly hidden (the current code already checks `provider.models.length > 0`, just ensure it uses the filtered length).
  </action>
  <verify>
    Check that bookmarked models only show in one place.
  </verify>
  <done>
    Bookmarked models are removed from provider sections and only show in "Bookmarked".
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify exclusive bookmark visibility</name>
  <files>src/components/chat/ModelSelector.tsx</files>
  <action>Human verification of the model selector behavior.</action>
  <verify>Human confirms the behavior is correct.</verify>
  <done>Behavior confirmed by human.</done>
  <what-built>Exclusive bookmark visibility in ModelSelector</what-built>
  <how-to-verify>
    1. Open the ModelSelector.
    2. Bookmark a model (e.g., from OpenAI).
    3. Verify it appears in "Bookmarked" and disappears from "OpenAI" section.
    4. Unbookmark it.
    5. Verify it disappears from "Bookmarked" and reappears in "OpenAI" section.
    6. Click "Refresh Models" and verify visibility rules persist.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Frontend → Backend (Tauri IPC): Config persistence

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Config storage | mitigate | Backend merges config updates to prevent accidental data loss. |
</threat_model>

<success_criteria>
ModelSelector maintains a clean, non-redundant list where bookmarked models are prioritized and deduplicated from provider lists.
</success_criteria>

<output>
After completion, create `.planning/phases/08-model-selector-polish/08-01-SUMMARY.md`
</output>
