---
phase: "09.4-header-consolidation"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [src/components/dashboard/Dashboard.tsx, ROADMAP.md, .planning/STATE.md]
autonomous: true
requirements: [R-09.4-01]

must_haves:
  truths:
    - "User sees exactly one header strip (height 14) at the top of the dashboard."
    - "ModelSelector and Eject button are centered in the header."
    - "Right side contains Ghost (incognito), Bell, Separator, Theme Switch, Right Sidebar toggle, and WindowControls."
    - "Eject button only appears when `selectedProvider.startsWith('local_')`."
    - "All interactive elements have `no-drag` and remain clickable while the header remains a drag region."
  artifacts:
    - path: "src/components/dashboard/Dashboard.tsx"
      provides: "Unified header layout"
  key_links:
    - from: "src/components/dashboard/Dashboard.tsx"
      to: "/assets/eject.svg"
      via: "img src"
---

<objective>
Consolidate the dashboard header by removing the secondary bar and merging its content (Model Selector, Eject button, and Right Sidebar Toggle) into the top header strip. This creates a more compact, single-strip navigation experience.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/dashboard/Dashboard.tsx

<interfaces>
From src/components/dashboard/Dashboard.tsx:
```typescript
export function Dashboard() {
  const [selectedProvider, setSelectedProvider] = useState('anthropic');
  const [selectedModel, setSelectedModel] = useState('claude-3-5-sonnet-20240620');
  const [isIncognito, setIsIncognito] = useState(false);
  const [isRightSidebarOpen, setIsRightSidebarOpen] = useState(false);
  const [isLeftSidebarOpen, setIsLeftSidebarOpen] = useState(true);
  const [isDark, setIsDark] = useState(() => document.documentElement.classList.contains('dark'));
  const toggleIncognito = () => { /* ... */ };
  const handleThemeChange = (checked: boolean) => { /* ... */ };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md and STATE.md</name>
  <files>ROADMAP.md, .planning/STATE.md</files>
  <action>
    - Add Phase 09.4: Header Consolidation to ROADMAP.md with requirement [R-09.4-01].
    - Requirement R-09.4-01: "Header must be a single unified strip (h-14) containing all chat and system controls."
    - Update .planning/STATE.md to set Phase 09.4 as the current active phase.
  </action>
  <verify>grep "Phase 09.4" ROADMAP.md && grep "09.4" .planning/STATE.md</verify>
  <done>Roadmap and State are updated to reflect the new phase.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate Header Layout in Dashboard.tsx</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <action>
    - Delete the entire "SECOND BAR" block (including its wrapping div).
    - Modify the "TOP BAR" <header> to use a three-column flex structure:
      - Left column (flex-1): Flex items-center justify-start. Contains the left sidebar toggle button.
      - Center column (flex-1): Flex items-center justify-center. Contains the `ModelSelector` and the Eject button.
      - Right column (flex-1): Flex items-center justify-end. Contains Ghost (incognito), Bell, Separator (vertical), Switch (theme), Right Sidebar toggle, and WindowControls.
    - Implement the Eject button using `img src="/assets/eject.svg"`.
    - Eject button styling: `opacity-50 hover:opacity-100 transition-opacity` on the button. Use `h-9 w-9` for the button and `h-5 w-5` for the image.
    - Apply conditional rendering for the Eject button: `selectedProvider.startsWith('local_')`.
    - Ensure all interactive columns have the `no-drag` class.
    - Preserve `data-tauri-drag-region` on the `<header>`.
    - Preserve all state and event handlers (toggleIncognito, handleThemeChange, etc.).
    - Maintain existing background/border styling based on `isIncognito` state.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Header is consolidated into a single strip with all required controls and proper alignment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> UI | Header controls are untrusted input sources for state updates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.4-01 | S | WindowControls | mitigate | Ensure WindowControls are only interactive by the user and do not expose internal Tauri APIs unnecessarily. (Existing component) |
| T-09.4-02 | T | data-tauri-drag-region | mitigate | Ensure that `no-drag` is applied to all interactive elements to prevent drag events from swallowing click events. |
</threat_model>

<verification>
- Run `npx tsc --noEmit` to check for type errors.
- Manual check: Header height should be `h-14`.
- Manual check: Eject button should only appear when a local model is selected.
</verification>

<success_criteria>
- Dashboard has exactly one header bar of height 14.
- All functional controls from the previous two bars are present and working in the single header.
- The model selector is centered.
- The Eject button uses the correct SVG asset and conditional logic.
</success_criteria>

<output>
After completion, create `.planning/phases/09.4-header-consolidation/09.4-01-SUMMARY.md`
</output>
