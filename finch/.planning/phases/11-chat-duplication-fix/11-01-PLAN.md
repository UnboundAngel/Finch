---
phase: 11-chat-duplication-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/dashboard/Dashboard.tsx]
autonomous: true
requirements: [R-07-10]
user_setup: []

must_haves:
  truths:
    - "activeSessionId is tracked via a ref to prevent race conditions during first message saving"
    - "updateActiveSessionInList call in handleSend's onComplete is moved out of the setMessages functional update"
    - "no duplicate chat entries appear in the sidebar after the first message"
  artifacts:
    - path: "src/components/dashboard/Dashboard.tsx"
      provides: "Fixed chat session persistence logic"
  key_links:
    - from: "src/components/dashboard/Dashboard.tsx"
      to: "useRef"
      via: "React hook"
    - from: "src/components/dashboard/Dashboard.tsx"
      to: "activeSessionIdRef"
      via: "ref tracking"
---

<objective>
Fix chat duplication on the first message by using a ref to track the active session ID and moving the final save out of the state update cycle.

Purpose: Prevent duplicate chat entries in the sidebar that occur when the session ID state update is still pending when the subsequent AI stream completion save is triggered.
Output: Updated Dashboard.tsx with reliable persistence logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/dashboard/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement activeSessionIdRef and sync effect</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <action>
    - Import `useRef` from 'react'.
    - Near other state declarations, add `const activeSessionIdRef = React.useRef<string | null>(activeSessionId);`.
    - Add a `useEffect` to keep the ref in sync with the state: `React.useEffect(() => { activeSessionIdRef.current = activeSessionId; }, [activeSessionId]);`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Ref-tracking implemented and synced with state</done>
</task>

<task type="auto">
  <name>Task 2: Update updateActiveSessionInList and handleSend logic</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <action>
    - In `updateActiveSessionInList`:
      - Change `let currentSessionId = activeSessionId;` to `let currentSessionId = activeSessionIdRef.current;`.
      - Inside the `if (!currentSessionId)` block after `setActiveSessionId(savedId)`, add `activeSessionIdRef.current = savedId;`.
    - In `handleSend`'s `streamMessage`'s `onComplete` callback:
      - Extract the logic that constructs `finalMessages`.
      - Move the `updateActiveSessionInList(finalMessages)` call outside of `setMessages`.
      - Use a `setTimeout(() => updateActiveSessionInList(finalMessages), 0)` to ensure it runs with the final state but outside the React render cycle.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Persistence logic updated to avoid race conditions and side effects in state updates</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard.tsx | Internal UI logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | Session ID persistence | mitigate | Use ref-tracking to ensure session ID consistency |
</threat_model>

<verification>
Check for no TypeScript errors.
Manual test: Start a new chat, send one message, wait for completion, check for duplicates in sidebar.
</verification>

<success_criteria>
- Dashboard.tsx uses a ref for activeSessionId tracking.
- First message does not create duplicate entries in the sidebar.
- No TypeScript compilation errors.
</success_criteria>

<output>
After completion, create `.planning/phases/11-chat-duplication-fix/11-01-SUMMARY.md`
</output>
