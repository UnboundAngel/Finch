---
plan: "05-01"
name: Dashboard Empty State Polish
description: Refine the prompt cards and model selector on the dashboard.
phase: 05
type: autonomous
wave: 1
depends_on: []
files_modified:
  - "src/components/chat/ChatArea.tsx"
  - "src/components/chat/ModelSelector.tsx"
autonomous: true
requirements:
  - "R-01: Prompt cards must have equal height and 16px rounded corners."
  - "R-04: Model selector must use compact grouping and motion."
must_haves:
  truths:
    - "Cards: rounded-2xl (16px)"
    - "Model Selector: Framer Motion height/opacity (180ms)"
  artifacts:
    - file: "src/components/chat/ChatArea.tsx"
      provides: "2x2 prompt grid with identical card heights"
      min_lines: 100
    - file: "src/components/chat/ModelSelector.tsx"
      provides: "Grouped model dropdown with motion"
      min_lines: 50
  key_links:
    - "ModelSelector.tsx -> Dashboard.tsx via selectedModel prop"
---

# Phase 05-01: Dashboard Empty State Polish

## Tasks

<task>
<name>PROMPT CARDS REFINEMENT</name>
<files>
  - "src/components/chat/ChatArea.tsx"
</files>
<action>
- Update `rounded-xl` to `rounded-2xl` (16px) on the prompt buttons.
- Wrap buttons in a grid container using `grid-rows-[1fr_1fr]` (or `grid-rows-fr` for subgrids) to ensure identical height.
- Set `min-h-[140px]` and `flex-1` on each button component.
- Align icon/title headers using a fixed height or flex-basis.
</action>
<verify>
- Run `npm run dev`.
- Inspect cards in dashboard: ensure identical height in 2x2 grid.
- Confirm `rounded-2xl` class is applied.
</verify>
<done>All 4 prompt cards have identical height and 16px rounded corners.</done>
</task>

<task>
<name>MODEL SELECTOR REDESIGN</name>
<files>
  - "src/components/chat/ModelSelector.tsx"
</files>
<action>
- Redesign `DropdownMenuContent` for compact grouping.
- Add Framer Motion `height` and `opacity` animations for opening/closing (180ms).
- Ensure provider names are non-selectable headers.
</action>
<verify>
- Click model selector trigger.
- Confirm smooth expansion animation.
- Verify grouping is clear and provider headers are non-selectable.
</verify>
<done>Model selector uses grouped labels and animates smoothly with zero layout shift.</done>
</task>

## Verification
- Run `npm run dev` and visually verify all tasks.
- Check build with `npm run build`.
