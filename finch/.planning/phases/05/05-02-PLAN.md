---
plan: "05-02"
name: Settings & Models Polish
description: Smooth settings tab transitions and staggered provider animations.
phase: 05
type: autonomous
wave: 1
depends_on: ["05-01"]
files_modified:
  - "src/components/dashboard/SettingsDialog.tsx"
autonomous: true
requirements:
  - "R-02: Settings tabs must use AnimatePresence for non-simultaneous rendering."
  - "R-03: Provider cards must use staggered entry animations."
must_haves:
  truths:
    - "Transitions: 200ms ease-out"
    - "Stagger: 0.06s"
  artifacts:
    - file: "src/components/dashboard/SettingsDialog.tsx"
      provides: "Smooth tab switches and staggered card entry"
      min_lines: 200
  key_links:
    - "SettingsDialog.tsx -> ProviderSection internal logic"
---

# Phase 05-02: Settings & Models Polish

## Tasks

<task>
<name>SETTINGS TAB TRANSITIONS</name>
<files>
  - "src/components/dashboard/SettingsDialog.tsx"
</files>
<action>
- Use `AnimatePresence (mode="wait")` and `motion.div` for tab contents.
- Implement 200ms fade + 8px slide transition.
</action>
<verify>
- Switch between General and Models & Keys tabs.
- Confirm outgoing tab exits before incoming tab enters.
</verify>
<done>Settings tabs transition smoothly with AnimatePresence.</done>
</task>

<task>
<name>MODELS SCREEN STAGGERED ANIMATIONS</name>
<files>
  - "src/components/dashboard/SettingsDialog.tsx"
</files>
<action>
- Convert `ProviderSection` to a `motion.div`.
- Add staggered entry variants to the container (0.06s delay per child).
- Apply interactive hover effects (`scale: 1.01`, border highlight).
</action>
<verify>
- Open Models & Keys tab.
- Confirm sequential card animation.
- Verify hover states function as intended.
</verify>
<done>Provider cards use staggered entry and interactive hover states.</done>
</task>

## Verification
- Run `npm run dev` and visually verify all tasks.
- Check build with `npm run build`.
