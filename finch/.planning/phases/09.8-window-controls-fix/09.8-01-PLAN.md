---
phase: "09.8-window-controls-fix"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/components/dashboard/WindowControls.tsx"]
autonomous: true
requirements: ["WINDOW-FIX-01"]
must_haves:
  truths:
    - "WindowControls uses Tauri v2 currentMonitor API"
    - "Minimize and Close buttons do not cause nested button errors in tooltips"
  artifacts:
    - path: "src/components/dashboard/WindowControls.tsx"
      provides: "Fixed window controls"
  key_links:
    - from: "src/components/dashboard/WindowControls.tsx"
      to: "@tauri-apps/api/window"
      via: "import { getCurrentWindow, currentMonitor }"
---

<objective>
Fix WindowControls.tsx for Tauri v2 API and nested button errors in tooltips.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/components/dashboard/WindowControls.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update WindowControls imports and snapTo function</name>
  <files>src/components/dashboard/WindowControls.tsx</files>
  <action>
    - Update imports from '@tauri-apps/api/window' to include 'currentMonitor'.
    - In the 'snapTo' function, change 'const monitor = await appWindow.currentMonitor();' to 'const monitor = await currentMonitor();'.
  </action>
  <verify>
    <automated>grep "import { getCurrentWindow, currentMonitor } from '@tauri-apps/api/window';" src/components/dashboard/WindowControls.tsx && grep "const monitor = await currentMonitor();" src/components/dashboard/WindowControls.tsx</automated>
  </verify>
  <done>Tauri v2 API used for monitor detection.</done>
</task>

<task type="auto">
  <name>Task 2: Fix nested button errors in TooltipTrigger</name>
  <files>src/components/dashboard/WindowControls.tsx</files>
  <action>
    - For Minimize and Close TooltipTrigger components, replace 'asChild' with 'render' prop.
    - Pass the button JSX directly to the 'render' prop as specified in the instructions.
  </action>
  <verify>
    <automated>grep "TooltipTrigger render={" src/components/dashboard/WindowControls.tsx</automated>
  </verify>
  <done>Nested button errors resolved by using render prop.</done>
</task>

<task type="auto">
  <name>Task 3: Final verification and push</name>
  <files>src/components/dashboard/WindowControls.tsx</files>
  <action>
    - Run type checking: 'npx tsc --noEmit'.
    - Commit the changes and push to GitHub.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Code verified and pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Tauri API | Interaction with OS-level window management |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.8-01 | Elevation of Privilege | WindowControls | mitigate | Ensure only window-level commands are executed and that they are permitted by Tauri capabilities. |
</threat_model>

<success_criteria>
- WindowControls.tsx uses the correct Tauri v2 API for monitor access.
- Tooltip triggers are implemented using the 'render' prop to avoid hydration/nested element errors.
- Project passes TypeScript verification.
</success_criteria>
