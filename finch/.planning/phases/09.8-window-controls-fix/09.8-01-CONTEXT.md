# Phase 09.8 - Window Controls Fix

## Goals
Fix `WindowControls.tsx` for Tauri v2 API and nested button errors in tooltips.

## Decisions
- D-01: Update `@tauri-apps/api/window` imports to include `currentMonitor`.
- D-02: Use `currentMonitor()` directly instead of `appWindow.currentMonitor()` in `snapTo`.
- D-03: Use `render` prop on `TooltipTrigger` (from `@base-ui/react`) to avoid nested `<button>` elements.

## Deferred Ideas
None.

## the agent's Discretion
None.
