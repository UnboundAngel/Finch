---
phase: 09.5-hotfixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/dashboard/Dashboard.tsx, src/hooks/useChatPersistence.ts]
autonomous: true
requirements: [R-09.5-01, R-09.5-02]
must_haves:
  truths:
    - "Left sidebar toggle icons load without being blank"
    - "Chat migration completes successfully without crashing"
    - "TypeScript check passes"
  artifacts:
    - path: "src/components/dashboard/Dashboard.tsx"
      provides: "Fixed sidebar toggle icons"
    - path: "src/hooks/useChatPersistence.ts"
      provides: "Robust chat migration logic"
  key_links:
    - from: "src/hooks/useChatPersistence.ts"
      to: "Rust backend"
      via: "invoke('save_chat')"
      pattern: "invoke\\('save_chat'"
---

<objective>
Fix the blank left sidebar icon by ensuring correct asset paths and resolve a crash during chat migration by providing missing timestamps.

Purpose: Improve UI reliability and data migration stability.
Output: Fixed sidebar toggle and robust chat migration logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/components/dashboard/Dashboard.tsx
@src/hooks/useChatPersistence.ts
@src/types/chat.ts

<interfaces>
From src/types/chat.ts:
```typescript
export type ChatSession = {
  id: string;
  title: string;
  messages: Message[];
  model?: string;
  provider?: string;
  timestamp: number;
  created_at?: number;
  updated_at?: number;
  // ...
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Left Sidebar Toggle Icon</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <action>
    Locate the left sidebar toggle Button in the header (around line 380).
    Verify that the `img` `src` attribute uses `/assets/open-state-left.svg` and `/assets/closed-state-left.svg` with a leading slash.
    If they are missing the leading slash, add it.
    The goal is to ensure the paths are `/assets/open-state-left.svg` and `/assets/closed-state-left.svg`.
  </action>
  <verify>
    <automated>grep "/assets/open-state-left.svg" src/components/dashboard/Dashboard.tsx && grep "/assets/closed-state-left.svg" src/components/dashboard/Dashboard.tsx</automated>
  </verify>
  <done>Left sidebar toggle icons have leading slashes in their src paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix Chat Migration Crash</name>
  <files>src/hooks/useChatPersistence.ts</files>
  <behavior>
    - When `legacyChats` are found in localStorage, each chat must be migrated to the new file-based format.
    - The `migratedChat` object must include a `created_at` field.
    - If `chat.created_at` is missing in the legacy chat, it should default to `Date.now()`.
  </behavior>
  <action>
    In the `loadAndMigrate` function within `useChatPersistence.ts`, locate the `migratedChat` object construction inside the loop over `legacyChats`.
    Add `created_at: chat.created_at || Date.now()` to the `migratedChat` object.
    This ensures compatibility with the Rust backend which expects an `i64` for `created_at`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep "created_at: chat.created_at || Date.now()" src/hooks/useChatPersistence.ts</automated>
  </verify>
  <done>Chat migration logic now includes the required `created_at` timestamp.</done>
</task>

<task type="auto">
  <name>Task 3: Final Verification and Push</name>
  <files></files>
  <action>
    Run a final TypeScript check to ensure no regressions.
    Commit the changes and push to GitHub.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Changes verified and pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| localStorage → migration logic | Untrusted legacy data is parsed and migrated. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.5-01 | Information Disclosure | Chat Migration | mitigate | Ensure only non-incognito chats are migrated (already handled). |
| T-09.5-02 | Denial of Service | Chat Migration | mitigate | Provide fallback for missing timestamps to prevent backend deserialization errors. |
</threat_model>

<success_criteria>
- Left sidebar toggle icons load correctly (paths have leading slashes).
- Chat migration no longer crashes due to missing `created_at` field.
- Code passes type check.
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-hotfixes/09.5-01-SUMMARY.md`
</output>
