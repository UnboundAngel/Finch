---
phase: 09.6-icon-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/components/dashboard/Dashboard.tsx"]
autonomous: true
requirements: ["R-09.6-01"]
must_haves:
  truths:
    - "Left sidebar toggle icon (open/closed-state-left.svg) has 'dark:invert' class"
    - "Eject icon (eject.svg) has 'dark:invert' class"
    - "Right sidebar toggle icon (open/closed-state-right.svg) has 'dark:invert' class"
  artifacts:
    - path: "src/components/dashboard/Dashboard.tsx"
      provides: "Updated dashboard with dark-mode icons"
---

<objective>
Fix invisible SVG icons in dark mode in src/components/dashboard/Dashboard.tsx by adding the `dark:invert` class to key `<img>` tags.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/components/dashboard/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dark:invert to Dashboard icons</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <action>
    Add the `dark:invert` class to the following `<img>` tags in `src/components/dashboard/Dashboard.tsx`:
    1. Left sidebar toggle icon (approx line 389): Add `dark:invert` to the className.
    2. Eject icon (approx line 416): Add `dark:invert` to the className.
    3. Right sidebar toggle icon (approx line 446): Add `dark:invert` to the className.
    
    Maintain all existing classes and logic.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    `src/components/dashboard/Dashboard.tsx` contains `dark:invert` on the specified `<img>` tags and compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit and push changes</name>
  <files>None</files>
  <action>
    Commit the changes to the repository with a descriptive message and push to GitHub.
  </action>
  <verify>
    <automated>git status</automated>
  </verify>
  <done>
    Changes are pushed to GitHub.
  </done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.6-01 | Tampering | UI/Dashboard | mitigate | No logic change, only style classes added. |
</threat_model>

<success_criteria>
- [ ] Left toggle icon has `dark:invert` class.
- [ ] Eject icon has `dark:invert` class.
- [ ] Right toggle icon has `dark:invert` class.
- [ ] TypeScript compilation passes.
</success_criteria>

<output>
After completion, create `.planning/phases/09.6-icon-fixes/09.6-01-SUMMARY.md`
</output>
