---
phase: "09.1-header-refinement"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["src/components/dashboard/Dashboard.tsx"]
autonomous: true
requirements: ["R-09.1-01", "R-09.1-02"]

must_haves:
  truths:
    - "Header shows SidebarToggleButton and ModelSelector on the left."
    - "Header shows Incognito Toggle and Right Sidebar Toggle in a 'Chat Controls' group."
    - "A vertical separator exists between Chat Controls and System Controls."
    - "System Controls contains Theme Switch and WindowControls."
    - "Right sidebar correctly toggles and shifts layout using functional state update."
  artifacts:
    - path: "src/components/dashboard/Dashboard.tsx"
      provides: "Updated header layout and toggle behavior"
  key_links:
    - from: "Right Sidebar Toggle"
      to: "setIsRightSidebarOpen"
      via: "functional update: prev => !prev"
---

<objective>
Fix sidebar toggle placement and close behavior in Dashboard.tsx.
- Move the left sidebar toggle button out of the window controls area (if it was there, though it seems it's on the left already, but the user wants to reorganize the whole right side anyway).
- Reorganize the header into three logical groups: Left, Right Chat Controls, and Right System Controls.
- Add a vertical separator between Chat Controls and System Controls.
- Fix the Right Sidebar toggle to use functional state updates.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/dashboard/Dashboard.tsx
@components/ui/separator.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dashboard.tsx Header and Toggle Logic</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <action>
    1. Import `Separator` from `@/components/ui/separator`.
    2. Update the header JSX structure:
       - Left div (existing): Keep `SidebarToggleButton`, `Incognito` label, and `ModelSelector`.
       - Right div: Split into two groups separated by a vertical `Separator`.
       - Group 1 (Chat Controls): `Incognito Toggle` (Ghost button), `Right Sidebar Toggle` (the one with img).
       - Group 2 (System Controls): `Theme Switch` (Switch), `WindowControls`.
       - Add `Separator` between Group 1 and Group 2.
    3. Change the Right Sidebar toggle `onClick` handler to use `setIsRightSidebarOpen(prev => !prev)`.
    4. Ensure the `mr-4` margin is on the outermost right container or handled gracefully between groups.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Header organized into 3 groups with separator; functional toggle implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Commit changes</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <action>
    Commit the changes with a message like "fix(header): reorganize header groups and fix sidebar toggle behavior".
  </action>
  <verify>
    <automated>git status</automated>
  </verify>
  <done>Changes committed to the repository.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Header UI | User interaction with toggles |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-01 | I | Dashboard Header | mitigate | Ensure no sensitive info leaked in header (already handled by Incognito mode) |
</threat_model>

<success_criteria>
- Header layout matches user description.
- Right sidebar toggle works reliably (prevents race conditions via functional update).
- Layout shift works as expected (handled by flex-1 and min-w-0 on main).
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-header-refinement/09.1-01-SUMMARY.md`
</output>
