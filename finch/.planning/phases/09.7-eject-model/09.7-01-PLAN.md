---
phase: 09.7-eject-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src-tauri/capabilities/default.json, src-tauri/permissions/finch.toml, src/components/dashboard/Dashboard.tsx]
autonomous: true
requirements: [R-09.7-01, R-09.7-02]

must_haves:
  truths:
    - "Clicking eject button invokes the `eject_model` command"
    - "Model and provider selection are cleared after ejection"
    - "Tauri permits the `eject_model` command"
  artifacts:
    - path: "src-tauri/capabilities/default.json"
      provides: "Permission enablement"
    - path: "src-tauri/permissions/finch.toml"
      provides: "Permission definition"
    - path: "src/components/dashboard/Dashboard.tsx"
      provides: "Eject button logic"
  key_links:
    - from: "src/components/dashboard/Dashboard.tsx"
      to: "eject_model"
      via: "tauri invoke"
      pattern: "invoke\\('eject_model'"
---

<objective>
Implement the `eject_model` command permissions and wire it to the frontend eject button.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src-tauri/src/lib.rs
@src-tauri/capabilities/default.json
@src-tauri/permissions/finch.toml
@src/components/dashboard/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Tauri Permissions</name>
  <files>src-tauri/capabilities/default.json, src-tauri/permissions/finch.toml</files>
  <action>
    - Add `"allow-eject-model"` to the `permissions` array in `src-tauri/capabilities/default.json`.
    - Add the `allow-eject-model` permission definition to `src-tauri/permissions/finch.toml`:
      ```toml
      [[permission]]
      identifier = "allow-eject-model"
      description = "Allows the eject_model command"
      commands.allow = ["eject_model"]
      ```
  </action>
  <verify>
    <automated>Select-String -Path "src-tauri/capabilities/default.json" -Pattern "allow-eject-model" ; Select-String -Path "src-tauri/permissions/finch.toml" -Pattern "allow-eject-model"</automated>
  </verify>
  <done>Permissions added to both files.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Eject Button to Command</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <action>
    Update the eject button's `onClick` handler to use the `eject_model` command.
    Ensure to use `model_id` (snake_case) as the key to match the Rust command signature.
    Implementation should handle errors and clear selection in the `finally` block.
    
    Code to implement:
    ```tsx
    onClick={async () => {
      try {
        await invoke('eject_model', { 
          provider: selectedProvider, 
          model_id: selectedModel 
        });
      } catch (err) {
        console.error('Failed to eject model:', err);
        toast.error('Failed to eject model');
      } finally {
        setSelectedModel('');
        setSelectedProvider('');
      }
    }}
    ```
  </action>
  <verify>
    <automated>Select-String -Path "src/components/dashboard/Dashboard.tsx" -Pattern "invoke\('eject_model'"</automated>
  </verify>
  <done>Eject button wired to Rust command.</done>
</task>

<task type="auto">
  <name>Task 3: Push to GitHub</name>
  <files></files>
  <action>
    Commit and push the changes to GitHub origin main.
  </action>
  <verify>
    <automated>git push origin main</automated>
  </verify>
  <done>Changes pushed to remote.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend→Rust | JS calls Rust command `eject_model` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.7-01 | I | eject_model | mitigate | Ensure parameters are strings and command is only accessible via defined permission. |
</threat_model>

<verification>
Check `src-tauri/capabilities/default.json`, `src-tauri/permissions/finch.toml`, and `src/components/dashboard/Dashboard.tsx` for correctness.
</verification>

<success_criteria>
- `allow-eject-model` present in Tauri config.
- `Dashboard.tsx` eject button uses `invoke('eject_model', ...)`.
- Selection cleared on eject.
- Changes pushed to GitHub.
</success_criteria>

<output>
After completion, create `.planning/phases/09.7-eject-model/09.7-01-SUMMARY.md`
</output>
