---
phase: 09.2-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/dashboard/Dashboard.tsx]
autonomous: true
requirements: [R-09.1-01, R-09.1-02]
must_haves:
  truths:
    - "Model selector is horizontally centered in a second bar below the main header"
    - "Right sidebar toggle is moved to the second bar (right zone)"
    - "Eject button (Lucide Eject icon) only appears when a local model is selected"
    - "Clicking Eject clears selectedModel and selectedProvider"
    - "Top bar remains exactly as-is regarding drag region, window controls, and theme switch"
  artifacts:
    - path: "src/components/dashboard/Dashboard.tsx"
      provides: "Refined header and model control bar"
  key_links:
    - from: "src/components/dashboard/Dashboard.tsx"
      to: "src/components/chat/ModelSelector.tsx"
      via: "Component import and prop passing"
---

<objective>
Refactor the dashboard layout to separate model/chat controls into a dedicated second bar, leaving the top bar for window/system controls and drag operations.

Purpose: Clearer separation of concerns and a more spacious layout.
Output: Updated Dashboard.tsx with a two-bar header structure.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/components/dashboard/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create second bar and move components</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <action>
    - Add `Eject` to the imports from `lucide-react`.
    - Modify `Dashboard.tsx` to insert a new `div` between the `<header>` and `<ChatArea>`.
    - Styling for the new bar: `h-12 flex items-center justify-between px-4 border-b border-muted-foreground/10 bg-background/50 backdrop-blur-md z-10 sticky top-14`.
    - Structure of the second bar:
        - Left: Empty `div` with `flex-1`.
        - Center: `div` with `flex-1 flex justify-center` containing `<ModelSelector ... />`.
        - Right: `div` with `flex-1 flex items-center justify-end gap-2` containing the Unload button and Right Sidebar toggle.
    - Unload button implementation:
        - Only render if `selectedProvider.startsWith('local_')`.
        - Use Lucide `Eject` icon.
        - On click: `setSelectedModel('')` and `setSelectedProvider('')`.
    - Right Sidebar toggle: Move the existing `<Button>` for `isRightSidebarOpen` from the top bar to this right zone.
  </action>
  <verify>
    <automated>grep -q "Eject" src/components/dashboard/Dashboard.tsx && grep -q "h-12 flex items-center" src/components/dashboard/Dashboard.tsx</automated>
  </verify>
  <done>Second bar created with centered ModelSelector, conditional Eject button, and Right Sidebar toggle.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up top bar</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <action>
    - Remove `<ModelSelector />` from the header.
    - Remove the right sidebar toggle `<Button>` from the header.
    - Ensure the header structure remains:
        - Left: `SidebarToggleButton`, `Incognito` label.
        - Right: `Incognito Toggle`, `Separator`, `Switch`, `WindowControls`.
    - Keep `data-tauri-drag-region` on the header.
  </action>
  <verify>
    <automated>! grep -q "<ModelSelector" src/components/dashboard/Dashboard.tsx | head -n 1 | grep "header"</automated>
  </verify>
  <done>Top bar cleaned up, keeping only window controls, theme switch, incognito toggle, and sidebar toggle button.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-01 | Tampering | UI State | mitigate | Use functional state updates for `isRightSidebarOpen` to ensure consistency. |
</threat_model>

<verification>
Manual verification:
1. Run `npm run dev`.
2. Check if ModelSelector is now centered in a new bar.
3. Select a local provider (Ollama/LM Studio) and verify the Eject button appears.
4. Click Eject and verify the model/provider are cleared.
5. Verify the top bar still allows dragging the window and has window controls.
6. Verify the right sidebar toggle still works from its new position.
</verification>

<success_criteria>
- [ ] ModelSelector moved to a second bar and horizontally centered.
- [ ] Right sidebar toggle moved to the second bar.
- [ ] Eject button added to second bar, conditionally visible for local models.
- [ ] Top bar retains drag functionality and system controls.
</success_criteria>

<output>
After completion, create `.planning/phases/09.2-fixes/09.2-01-SUMMARY.md`
</output>
