---
phase: 10-codebase-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src-tauri/src/lib.rs, src/components/dashboard/Dashboard.tsx]
autonomous: true
requirements: [REF-01, REF-02]
must_haves:
  truths:
    - "lib.rs is reduced from ~1400 lines to a slim entry point"
    - "Dashboard.tsx is reduced from ~1000 lines to a modular layout component"
    - "All existing Tauri IPC commands remain functional with identical signatures"
    - "No data loss for chat sessions or provider configurations"
    - "Code style, types, and glassmorphic aesthetics are preserved"
  artifacts:
    - path: "src-tauri/src/ipc/"
      provides: "Modularized command handlers"
    - path: "src-tauri/src/providers/"
      provides: "Encapsulated AI provider logic"
    - path: "src/hooks/"
      provides: "Extracted dashboard behavior"
    - path: "src/providers/ModalProvider.tsx"
      provides: "Centralized modal state management"
  key_links:
    - from: "src/components/dashboard/Dashboard.tsx"
      to: "src/hooks/useDynamicBackground.ts"
      via: "Custom hook for UI contrast"
---

<objective>
Refactor the Finch codebase to resolve technical debt in the monolithic lib.rs and Dashboard.tsx files. This involves a structural split into specialized modules, custom hooks, and providers without altering the underlying logic or user experience.

Purpose: Improve maintainability, reduce cognitive load, and enable parallel development.
Output: A modular backend (Rust) and a cleaner frontend (React).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@docs/Migration-Map-Rust.md
@docs/Migration-Map-React.md
@docs/Migration-Map-Integration.md
@src-tauri/src/lib.rs
@src/components/dashboard/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rust - Extract Types and State</name>
  <files>src-tauri/src/types.rs, src-tauri/src/lib.rs</files>
  <action>
    - Create `src-tauri/src/types.rs`.
    - Move `AppState`, `ChatMessage`, `ProviderConfig`, `SourceEntry`, `StreamingEvent`, `HardwareInfo`, and `ContextIntelligence` to `types.rs`.
    - Ensure all moved items are `pub`.
    - Update `lib.rs` to use `mod types;`.
  </action>
  <verify>
    <automated>test -f src-tauri/src/types.rs && grep -q "pub struct AppState" src-tauri/src/types.rs</automated>
  </verify>
  <done>Core types and state extracted to a dedicated module.</done>
</task>

<task type="auto">
  <name>Task 2: Rust - Extract Session CRUD</name>
  <files>src-tauri/src/session.rs, src-tauri/src/lib.rs</files>
  <action>
    - Create `src-tauri/src/session.rs`.
    - Move `ChatSession` struct and the logic for `list_chats`, `load_chat`, `save_chat`, `delete_chat`.
    - Update `lib.rs` to use `mod session;`.
  </action>
  <verify>
    <automated>test -f src-tauri/src/session.rs && grep -q "fn save_chat" src-tauri/src/session.rs</automated>
  </verify>
  <done>Session management logic moved to session.rs.</done>
</task>

<task type="auto">
  <name>Task 3: Rust - Modularize AI Providers</name>
  <files>src-tauri/src/providers/</files>
  <action>
    - Create `src-tauri/src/providers/mod.rs`, `openai.rs`, `gemini.rs`, `local.rs`.
    - Move token estimation and history trimming to `providers/mod.rs`.
    - Move provider-specific request/streaming logic from `send_message` and `stream_message` to their respective files.
  </action>
  <verify>
    <automated>test -d src-tauri/src/providers && test -f src-tauri/src/providers/openai.rs</automated>
  </verify>
  <done>AI provider logic encapsulated into dedicated files.</done>
</task>

<task type="auto">
  <name>Task 4: Rust - Extract Config and Hardware</name>
  <files>src-tauri/src/config.rs, src-tauri/src/lib.rs</files>
  <action>
    - Create `src-tauri/src/config.rs`.
    - Move `get_provider_config`, `save_provider_config`, `update_search_config`, `set_background_image`, and `get_hardware_info`.
    - Update `lib.rs` to use `mod config;`.
  </action>
  <verify>
    <automated>test -f src-tauri/src/config.rs && grep -q "fn get_hardware_info" src-tauri/src/config.rs</automated>
  </verify>
  <done>Configuration and hardware telemetry extracted.</done>
</task>

<task type="auto">
  <name>Task 5: Rust - Final lib.rs Cleanup and IPC Registry</name>
  <files>src-tauri/src/lib.rs, src-tauri/src/ipc/</files>
  <action>
    - Create `src-tauri/src/ipc/` directory and sub-files as mapped.
    - Move remaining command handlers into the IPC layer.
    - Clean up `lib.rs` to be a pure entry point that registers modules and runs the Tauri builder.
  </action>
  <verify>
    <automated>grep -c "tauri::command" src-tauri/src/lib.rs | grep "^0$"</automated>
  </verify>
  <done>lib.rs refactored into a slim module registry.</done>
</task>

<task type="auto">
  <name>Task 6: React - Extract Custom Hooks</name>
  <files>src/hooks/</files>
  <action>
    - Create `useModelPolling.ts`, `useDynamicBackground.ts`, and `useChatSession.ts`.
    - Extract identified `useEffect` and callback logic from `Dashboard.tsx`.
  </action>
  <verify>
    <automated>test -f src/hooks/useDynamicBackground.ts</automated>
  </verify>
  <done>Dashboard behavior logic moved to reusable custom hooks.</done>
</task>

<task type="auto">
  <name>Task 7: React - Create Modal Provider</name>
  <files>src/providers/ModalProvider.tsx</files>
  <action>
    - Create `ModalProvider.tsx` with a context for managing Profile, Settings, and Overflow modals.
    - Export `useModals` hook.
  </action>
  <verify>
    <automated>grep -q "createContext" src/providers/ModalProvider.tsx</automated>
  </verify>
  <done>Modal state centralized and moved out of the Dashboard.</done>
</task>

<task type="auto">
  <name>Task 8: React - Split UI Components</name>
  <files>src/components/dashboard/</files>
  <action>
    - Create `DashboardHeader.tsx` and `DashboardMain.tsx`.
    - Move identified JSX blocks and relevant props from `Dashboard.tsx`.
  </action>
  <verify>
    <automated>test -f src/components/dashboard/DashboardHeader.tsx</automated>
  </verify>
  <done>Layout components separated for better readability.</done>
</task>

<task type="auto">
  <name>Task 9: React - Reassemble Dashboard</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <action>
    - Update `Dashboard.tsx` to use the new hooks, provider, and layout components.
    - Ensure all prop drilling and state synchronization follows the integration map.
  </verify>
  <verify>
    <automated>grep -q "useDynamicBackground" src/components/dashboard/Dashboard.tsx</automated>
  </verify>
  <done>Dashboard reassembled as a lean orchestrator.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | IPC Signatures | mitigate | Use automated grep checks to ensure every command name in `lib.rs`'s old `invoke_handler` exists in the new structure. |
| T-10-02 | Data Loss | Chat Sessions | mitigate | Manual verification of chat history persistence after refactor. |
</threat_model>

<verification>
Manual verification:
1. Build and run the app.
2. Verify all chat history is still present.
3. Verify all AI providers (Anthropic, Gemini, Local) still stream correctly.
4. Verify theme switching and custom backgrounds still work.
5. Verify dynamic contrast (luminance check) still updates on background change.
6. Verify window controls and drag regions are still functional.
7. Verify all modals (Settings, Profile, Overflow) open and close correctly.
</verification>

<success_criteria>
- [ ] lib.rs is under 150 lines.
- [ ] Dashboard.tsx is under 200 lines.
- [ ] App compiles and runs without IPC errors.
- [ ] All features (Chat, Voice, Search, Settings) are functionally identical.
- [ ] No regression in glassmorphic UI or performance.
</success_criteria>

<output>
After completion, create `.planning/phases/10-codebase-refactor/10-01-SUMMARY.md`
</output>
