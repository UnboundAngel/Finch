---
phase: "10"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/components/sidebar/hooks/useSidebarTheme.ts",
  "src/components/sidebar/components/ParameterZone.tsx",
  "src/components/sidebar/hooks/useParameterGradients.ts"
]
autonomous: true
requirements: ["RS-SPLIT-01"]
must_haves:
  truths:
    - "Theme observer correctly identifies light/dark/Susie modes without flash"
    - "ParameterZone correctly handles collapsible state and animations"
    - "Gradients for Sampling section are computed correctly via hook"
  artifacts:
    - path: "src/components/sidebar/hooks/useSidebarTheme.ts"
      provides: "Theme-aware style constants"
    - path: "src/components/sidebar/components/ParameterZone.tsx"
      provides: "Shared collapsible section UI"
    - path: "src/components/sidebar/hooks/useParameterGradients.ts"
      provides: "Gradient calculation logic"
  key_links:
    - from: "src/components/sidebar/hooks/useSidebarTheme.ts"
      to: "document.documentElement"
      via: "MutationObserver"
---

<objective>
Establish the foundation for the Right Sidebar split by extracting shared hooks and the base UI wrapper. This ensures theme consistency and reduces code duplication across the extracted sections.

Purpose: Decouple theme/styling logic and section wrapping from the main component.
Output: useSidebarTheme, ParameterZone, and useParameterGradients.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/sidebar/RightSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useSidebarTheme hook</name>
  <files>src/components/sidebar/hooks/useSidebarTheme.ts</files>
  <action>
    Extract the theme observation and style constant logic from RightSidebar.tsx.
    - Implement a MutationObserver on document.documentElement to track 'class' changes.
    - Compute and return: textColor, mutedTextColor, inputBg, borderColor, iconColor, and circleBorderClass.
    - Accept isPinkMode and contrast as inputs.
  </action>
  <verify>
    <automated>Check if file exists and exports the hook with expected constants.</automated>
  </verify>
  <done>useSidebarTheme hook is functional and ready for consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Create ParameterZone component</name>
  <files>src/components/sidebar/components/ParameterZone.tsx</files>
  <action>
    Extract the ParameterZone sub-component from the bottom of RightSidebar.tsx into its own file.
    - Ensure it uses Framer Motion for collapse/expand animations.
    - Support 'uncollapsible' prop for sections like Stop Words.
    - Ensure layout matches existing design exactly.
  </action>
  <verify>
    <automated>Verify component exports and Framer Motion usage.</automated>
  </verify>
  <done>ParameterZone is a standalone reusable component.</done>
</task>

<task type="auto">
  <name>Task 3: Create useParameterGradients hook</name>
  <files>src/components/sidebar/hooks/useParameterGradients.ts</files>
  <action>
    Extract getTemperatureGradient and getTopPGradient from RightSidebar.tsx.
    - The hook should take theme-related inputs (isPinkMode, contrast, etc.) and return the gradient calculation functions.
    - Ensure all color constants match the original implementation.
  </action>
  <verify>
    <automated>Verify hook returns two functions for gradient calculation.</automated>
  </verify>
  <done>Gradient logic is centralized and testable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DOM → Hook | MutationObserver reads from document.documentElement |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Information Disclosure | useSidebarTheme | accept | Only reading public theme classes from DOM |
</threat_model>

<verification>
Ensure all three files are created and export their respective members. No logic changes should be introduced during extraction.
</verification>

<success_criteria>
- useSidebarTheme.ts created
- ParameterZone.tsx created
- useParameterGradients.ts created
- All exports match the needs of the upcoming extraction phase.
</success_criteria>

<output>
After completion, create .planning/phases/10-right-sidebar-split/10-01-SUMMARY.md
</output>
