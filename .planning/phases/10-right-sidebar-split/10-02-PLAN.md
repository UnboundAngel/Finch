---
phase: "10"
plan: "02"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: [
  "src/components/sidebar/components/SystemPromptSection.tsx",
  "src/components/sidebar/components/StopWordsSection.tsx",
  "src/components/sidebar/components/SamplingSection.tsx",
  "src/components/sidebar/components/OutputSection.tsx"
]
autonomous: true
requirements: ["RS-SPLIT-02"]
must_haves:
  truths:
    - "Extracted sections render correctly with same layout/styles as before"
    - "Sections react to isOpen and onToggle props from RightSidebar"
    - "StopWordsSection remains functional (add/remove stop words)"
    - "SamplingSection gradients update correctly using the extracted hook"
  artifacts:
    - path: "src/components/sidebar/components/SystemPromptSection.tsx"
      provides: "System Prompt textarea and token count"
    - path: "src/components/sidebar/components/StopWordsSection.tsx"
      provides: "Stop string list management"
    - path: "src/components/sidebar/components/SamplingSection.tsx"
      provides: "Creativity (Temperature) and Focus (Top P) sliders"
    - path: "src/components/sidebar/components/OutputSection.tsx"
      provides: "Response length input and MaxTokensSlider"
  key_links:
    - from: "src/components/sidebar/components/SystemPromptSection.tsx"
      to: "src/store"
      via: "useModelParams"
    - from: "src/components/sidebar/components/SamplingSection.tsx"
      to: "src/components/sidebar/hooks/useParameterGradients.ts"
      via: "hook call"
---

<objective>
Extract the four main sections of the Right Sidebar into standalone components. This reduces the complexity of RightSidebar.tsx and allows for easier maintenance and testing of individual sections.

Purpose: Isolate section-specific logic and UI.
Output: SystemPromptSection, StopWordsSection, SamplingSection, OutputSection.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/sidebar/RightSidebar.tsx
@.planning/phases/10-right-sidebar-split/10-01-SUMMARY.md
</context>

<interfaces>
From src/components/sidebar/components/ParameterZone.tsx:
```typescript
interface ParameterZoneProps {
  label: string;
  icon?: React.ReactNode;
  isOpen: boolean;
  onToggle: () => void;
  children: React.ReactNode;
  mutedTextColor: string;
  iconColor: string;
  contrast?: 'light' | 'dark';
  isPinkMode?: boolean;
  uncollapsible?: boolean;
}
```

From src/components/sidebar/hooks/useSidebarTheme.ts:
```typescript
export function useSidebarTheme(isPinkMode: boolean | undefined, contrast: 'light' | 'dark' | undefined);
// Returns: { textColor, mutedTextColor, inputBg, borderColor, iconColor, circleBorderClass }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract SystemPromptSection and StopWordsSection</name>
  <files>
    src/components/sidebar/components/SystemPromptSection.tsx,
    src/components/sidebar/components/StopWordsSection.tsx
  </files>
  <action>
    - Extract the "Prompt" zone logic into SystemPromptSection.tsx. It should use useModelParams for systemPrompt and useSidebarTheme for colors.
    - Extract the "Stop Words" zone logic into StopWordsSection.tsx. It should manage its own input ref and use addStopString/removeStopString from store.
    - Both should wrap their content in ParameterZone.
  </action>
  <verify>
    <automated>Verify component exports and store usage.</automated>
  </verify>
  <done>Text-based sections are extracted.</done>
</task>

<task type="auto">
  <name>Task 2: Extract SamplingSection</name>
  <files>src/components/sidebar/components/SamplingSection.tsx</files>
  <action>
    Extract the "Sampling" zone (Creativity and Focus) into its own component.
    - Use useParameterGradients hook for the range slider backgrounds.
    - Handle local input state for temperature and topP text inputs (syncing on blur).
    - Maintain warning messages (AnimatePresence) for high/low values.
  </action>
  <verify>
    <automated>Verify component exports and usage of useParameterGradients.</automated>
  </verify>
  <done>Sampling section with complex gradients is extracted.</done>
</task>

<task type="auto">
  <name>Task 3: Extract OutputSection</name>
  <files>src/components/sidebar/components/OutputSection.tsx</files>
  <action>
    Extract the "Output" zone into OutputSection.tsx.
    - Include the Response Length input and the MaxTokensSlider component.
    - Use useSidebarTheme for coloring.
  </action>
  <verify>
    <automated>Verify component exports and MaxTokensSlider integration.</automated>
  </verify>
  <done>Output section is extracted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User → Input | User types into textarea, inputs, and sliders |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | SystemPromptSection | mitigate | Input is clamped/validated before store update |
</threat_model>

<verification>
Verify all four component files exist and export functional React components. Ensure they all import and use ParameterZone and useSidebarTheme correctly.
</verification>

<success_criteria>
- Four new component files in src/components/sidebar/components/
- Each component is self-contained and manages its own internal state (like input values)
- Components accept isOpen/onToggle props from parent.
</success_criteria>

<output>
After completion, create .planning/phases/10-right-sidebar-split/10-02-SUMMARY.md
</output>
