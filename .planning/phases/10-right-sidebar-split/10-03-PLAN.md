---
phase: "10"
plan: "03"
type: execute
wave: 3
depends_on: ["10-02"]
files_modified: ["src/components/sidebar/RightSidebar.tsx"]
autonomous: false
requirements: ["RS-SPLIT-03"]
must_haves:
  truths:
    - "RightSidebar.tsx is reduced to < 150 lines"
    - "Sidebar functionality (toggles, inputs, sliders) remains identical to original"
    - "Animations (collapsing/expanding) are smooth and flicker-free"
  artifacts:
    - path: "src/components/sidebar/RightSidebar.tsx"
      provides: "Refactored sidebar container"
---

<objective>
Complete the refactoring of RightSidebar.tsx by integrating the newly extracted components and removing redundant logic. This achieves the goal of a modular, maintainable sidebar.

Purpose: Consolidate the refactor and verify full functionality.
Output: A lean, modular RightSidebar.tsx.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/sidebar/RightSidebar.tsx
@.planning/phases/10-right-sidebar-split/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor RightSidebar.tsx</name>
  <files>src/components/sidebar/RightSidebar.tsx</files>
  <action>
    - Import and use the extracted components: SystemPromptSection, SamplingSection, OutputSection, StopWordsSection.
    - Pass necessary props: isOpen, readyToFetch, isPinkMode, contrast, and toggle handlers.
    - Remove the local ParameterZone definition and all extracted hooks/gradient logic from this file.
    - Ensure RightSidebar.tsx only handles layout, scroll area, and section visibility state.
  </action>
  <verify>
    <automated>npm run lint (ensure no broken imports or missing props)</automated>
  </verify>
  <done>RightSidebar.tsx is refactored and lean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Refactored Right Sidebar with extracted components</what-built>
  <how-to-verify>
    1. Open the app and toggle the right sidebar (Ctrl+\).
    2. Verify all sections (Prompt, Sampling, Output, Stop Words) are visible and collapsible.
    3. Test inputs: Change system prompt, move sliders, add stop words.
    4. Switch themes (Light, Dark, Susie) and verify gradients and text colors update instantly.
    5. Verify animations are smooth when sections expand/collapse.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Components → State | Components communicate back to RightSidebar state via onToggle |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Denial of Service | RightSidebar | accept | Logic simplified, less chance of infinite re-renders |
</threat_model>

<verification>
Perform a final code review to ensure no dead code remains in RightSidebar.tsx. Verify total line count is significantly reduced.
</verification>

<success_criteria>
- RightSidebar.tsx is < 150 lines (it was ~500 lines).
- All sidebar functionality is preserved.
- Codebase is more modular and follows the new architecture.
</success_criteria>

<output>
After completion, create .planning/phases/10-right-sidebar-split/10-03-SUMMARY.md
</output>
