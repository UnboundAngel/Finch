---
phase: 09-right-sidebar
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - src/components/sidebar/RightSidebar.tsx
autonomous: true
requirements: [R-20]
must_haves:
  truths:
    - "Adjusting parameters in the sidebar immediately applies them application-wide"
    - "Controls correctly enforce boundary constraints (e.g. max tokens limits, temperature ranges)"
    - "Sliders feature dynamic Tailwind coloration based on values"
  artifacts:
    - path: "src/components/sidebar/RightSidebar.tsx"
      provides: "Sidebar controls UI"
      contains: "TooltipProvider"
  key_links:
    - from: "src/components/sidebar/RightSidebar.tsx"
      to: "src/store/index.ts"
      via: "useModelParams"
---

<objective>
Implement Layer 3 (Right Sidebar UI Panel) for global model parameter configuration.

Purpose: Provide a visually consistent interface for editing System Prompt, Creativity, Focus, Response Length, and Stop Words without bloating the component with local state.
Output: Fully populated `RightSidebar.tsx` utilizing Lucide tooltips, native styled controls, and strictly Zustand bindings.
</objective>

<context>
@ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Sidebar Controls UI</name>
  <files>src/components/sidebar/RightSidebar.tsx</files>
  <action>
    - Import `useModelParams` from `src/store/index.ts`. Remove all traces of `useState` (e.g., `newStop`).
    - Build the 5 controls in this exact order. **CRITICAL OVERRIDE**: Ignore glassmorphism from GEMINI.md for this phase — match existing RightSidebar.tsx visual style exactly, no glassmorphism, no new surface treatments. Use 12px rounded corners for inputs, include focus rings, and apply hover lifts/micro-interactions.
    - 1) System Prompt: Native `<textarea>` with auto-resize (max 6 lines). Show live approximate token count below (char length / 4).
      2) Creativity (Temperature): Labeled native `<input type="range">`, 0.0–2.0, step 0.01. Apply track colors dynamically via Tailwind utility class mapping depending on value: 0.0–0.3 = red, 0.3–1.2 = green, 1.2–1.8 = amber, 1.8–2.0 = red.
      3) Focus (Top P): Labeled native `<input type="range">`, 0.0–1.0, step 0.01. 0.0–0.3 = red, 0.3–0.7 = amber, 0.7–1.0 = green.
      4) Response Length (Max Tokens): Labeled native `<input type="range">`, 1–8192, step 1. 1–256 = amber, 256–4096 = green, 4096–8192 = amber.
      5) Stop Words: Native `<input type="text">` styled via Tailwind. Use a React `useRef` to capture values and `onKeyDown` to push to `stopStrings` on Enter (clearing the ref input afterwards), strictly avoiding `useState`. Render current `stopStrings` as pill tags with Lucide `X` icons to remove.
    - Beside each control label, place a `HelpCircle` icon from `lucide-react`. Wrap it in shadcn's `TooltipProvider`, `Tooltip`, `TooltipTrigger`, and `TooltipContent` to provide helpful context on hover.
    - Ensure all colors are derived from Tailwind classes (no hardcoded hexes).
    - Only use `lucide-react` icons (no emojis).
  </action>
  <verify>
    <automated>npm run tsc -- --noEmit</automated>
  </verify>
  <done>Right sidebar contains all 5 styled inputs wired securely to Zustand.</done>
</task>

</tasks>
