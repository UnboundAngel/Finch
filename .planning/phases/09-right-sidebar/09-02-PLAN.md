---
phase: 09-right-sidebar
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/useParamsStore.ts
  - src/store/modelParamsSlice.ts
  - src/store/index.ts
  - src/components/dashboard/Dashboard.tsx
  - src/components/sidebar/RightSidebar.tsx
  - src/hooks/useAIStreaming.ts
  - src/lib/tauri.ts
  - src-tauri/src/lib.rs
  - src-tauri/src/anthropic.rs
autonomous: true
requirements: [R-19]
must_haves:
  truths:
    - "AI generations dynamically adapt to the selected system prompt and creativity parameters"
    - "The application retains user-configured AI parameters across sessions"
    - "Provider payload includes correctly formatted parameters per provider"
  artifacts:
    - path: "src/store/modelParamsSlice.ts"
      provides: "Zustand slice for model parameters"
    - path: "src/store/index.ts"
      provides: "Root Zustand store export"
    - path: "src-tauri/src/lib.rs"
      provides: "Rust command extension for sending requests"
  key_links:
    - from: "src/hooks/useAIStreaming.ts"
      to: "src-tauri/src/lib.rs"
      via: "invoke stream_message"
---

<objective>
Implement Layer 1 (Zustand Global Params Slice) and Layer 2 (Rust IPC Extension).

Purpose: Establish the global state for AI parameters using standard camelCase naming and extend the Rust backend to consume them and correctly format provider API requests.
Output: Clean `modelParamsSlice.ts`, Root `index.ts` store, and extended Rust payloads in `lib.rs`.
</objective>

<context>
@ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zustand Slice & Client Store Wiring</name>
  <files>src/store/useParamsStore.ts, src/store/modelParamsSlice.ts, src/store/index.ts, src/components/dashboard/Dashboard.tsx, src/components/sidebar/RightSidebar.tsx, src/hooks/useAIStreaming.ts, src/lib/tauri.ts</files>
  <action>
    - Delete `src/store/useParamsStore.ts` to clear legacy snake_case store.
    - Create `src/store/modelParamsSlice.ts` to define and export `createModelParamsSlice`. State shape MUST strictly be: `systemPrompt` (string), `temperature` (number), `topP` (number), `maxTokens` (number), `stopStrings` (string[]). Include set actions for all.
    - Create `src/store/index.ts` as the root store that creates the store using `createModelParamsSlice` and wraps it in the Zustand `persist` middleware with the key `"finch-model-params"`. Export `useModelParams`.
    - Update `src/components/dashboard/Dashboard.tsx` and `src/components/sidebar/RightSidebar.tsx` to use the new store and its camelCase properties.
    - Update `src/hooks/useAIStreaming.ts` so `GenerationParams` interface uses camelCase keys matching the state shape. Ensure params are passed to `stream_message`.
    - Update `sendMessage` in `src/lib/tauri.ts` to accept the new camelCase parameters (`systemPrompt`, `temperature`, `topP`, `maxTokens`, `stopStrings`) and pass them to the `send_message` Tauri invoke call. **CRITICAL OVERRIDE**: Confirm `src/lib/tauri.ts` passes all new params through as camelCase untouched - do not manually convert any key to snake_case in any file.
  </action>
  <verify>
    <automated>npm run tsc -- --noEmit</automated>
  </verify>
  <done>Store is implemented as a slice, persists to correct key, and application compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Rust IPC Payloads & Provider Adapters</name>
  <files>src-tauri/src/lib.rs, src-tauri/src/anthropic.rs</files>
  <action>
    - In `src-tauri/src/lib.rs`, ensure the `stop_strings: Option<Vec<String>>` argument in both `send_message` and `stream_message` commands remains `stop_strings` to correctly bind to the frontend's `stopStrings` parameter. Tauri will automatically convert camelCase to snake_case.
    - Update `AnthropicRequest` in `src-tauri/src/anthropic.rs` if needed, ensuring `stop_sequences` receives the `stop_strings` argument.
    - In `lib.rs` for Gemini provider (both `send_message` and `stream_message`): Remove the temporary "user/model" system prompt hack from the `contents` vector. Instead, construct a `systemInstruction` field (`{"parts": [{"text": sys}]}`) and add it dynamically to the request body JSON at the root level if `system_prompt` is provided. Map `temperature`, `topP`, `max_tokens` (to `maxOutputTokens`), and `stop_strings` (to `stopSequences`) into `generationConfig`.
    - In `lib.rs` for OpenAI/Local provider (both commands): Ensure `temperature`, `top_p`, `max_tokens`, and `stop_strings` (mapped to `stop` in JSON) map cleanly to the JSON payload. Prepending the system message is already handled, so leave it untouched.
    - Do not break existing API calls. If `stop_strings` or other params are None or empty, ensure they are omitted from the request body (do not send nulls). 
  </action>
  <verify>
    <automated>cd src-tauri && cargo check</automated>
  </verify>
  <done>Rust commands handle the parameter payload accurately for Anthropic, Gemini, OpenAI, and Local providers.</done>
</task>

</tasks>
